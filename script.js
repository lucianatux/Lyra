/*
let carousel = document.getElementById('carouselExampleIndicators');
let pausePlayButton = document.getElementById('pausePlayButton');
let isPaused = false;

  pausePlayButton.addEventListener('click', function() {
    if (isPaused) {
      carousel.carousel('cycle'); // Reanuda el carrusel
      isPaused = false;
    } else {
      carousel.carousel('pause'); // Pausa el carrusel
      isPaused = true;
    }
  });
  */